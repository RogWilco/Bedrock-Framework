
# Retrieve Available Binaries
for file in `ls`
do
	if [ ${file:0:8} == "bedrock-" ]; then
		BINARIES=("${BINARIES[@]}" "$file")
	fi
done

BINARIES="${BINARIES[@]}"


for bin in $BINARIES
do
	echo $bin
done